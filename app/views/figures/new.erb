<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Figure</title>
  </head>
  <body>
    <form action="/figures" method="post">
      <h1>Create New Figure:</h1>
      <br></br>
      <input type="text" name="figure[name]" id="figure_name" placeholder="Name">
      <br></br>
      <input type="text" name="title[name]" id="new_title" placeholder="Title">
      <br></br>
      <% Title.all.each do |title| %>
      <input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>" value="<%= title.id %>"><%= title.name %></input>
      <% end %>
      <br></br>
      <input type="text" name="landmark[name]" id="new_landmark" placeholder="Landmark">
      <br></br>
      <% Landmark.all.each do |landmark| %>
      <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%= landmark.id %>" value="<%= landmark.id %>"><%= landmark.name %></input>
      <% end %>
      <br></br>
      <input type="submit" id="Create New Figure" value="Create">

    </form>
  </body>
</html>
